import { db } from '../../firebase'
import { GameType } from '../../utils/definitions/games_types';
import { IGameOneVOneStore } from './i_game_one_v_one_store';




export class GameOneVOneFireStore implements IGameOneVOneStore {
  create(game_state: UvUGameState) {

  }
  delete(game_id: string) {

  }
  update(game_state: UvUGameState) {

  }
  get(game_id: string): UvUGameState {

  }
}